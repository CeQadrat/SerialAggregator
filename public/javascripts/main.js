(function (angular) {
    angular.module('app',[]);

    angular.module('app')
        .controller('MainController', mainCtrl);

    function mainCtrl() {
        const self = this;
        // self.series = {};
    }
})(window.angular);